// Import nessesary foundation plugins
import {
    // Abide,
    // Accordion,
    // AccordionMenu,
    // Drilldown,
    // Dropdown,
    // DropdownMenu,
    // Equalizer,
    // Interchange,
    // Magellan,
    OffCanvas,
    // Orbit,
    // ResponsiveMenu,
    // ResponsiveToggle,
    // Reveal,
    // Slider,
    // SmoothScroll,
    // Sticky,
    // Tabs,
    // Toggler,
    // Tooltip,
    // ResponsiveAccordionTabs
} from 'foundation-sites'

// Initialise default plugin
// new AccordionMenu( $('[data-accordion-menu]') )

export {
    Abide,
    Accordion,
    AccordionMenu,
    Drilldown,
    Dropdown,
    DropdownMenu,
    Equalizer,
    Interchange,
    Magellan,
    OffCanvas,
    Orbit,
    ResponsiveMenu,
    ResponsiveToggle,
    Reveal,
    Slider,
    SmoothScroll,
    Sticky,
    Tabs,
    Toggler,
    Tooltip,
    ResponsiveAccordionTabs
}
