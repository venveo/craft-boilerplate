.loader-spinner {
    border: 6px solid #E8EBED;
    border-top: 6px solid #0196FA;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    animation: loader-spin 2s linear infinite;
}

@keyframes loader-spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
}
